#include "GLPrograms.h"
#include "LogUtil.h"
#include <stdlib.h>
#include <cstring>
#include <GLES2/gl2ext.h>

GLuint GLPrograms::program(const char *vertical, const char *fragment) {
    return NULL;
}

GLuint GLPrograms::shader(GLuint type, const char *source) {
    GLuint shader = 0;
    shader = gl
    return NULL;
}
